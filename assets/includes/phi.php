<?php

    // Preventing Header Injection, or at least trying...

    //$whitelist = array('yourdomain1', 'yourdomain2', 'yourdomain3');
    
    
    
    
    
    
    //$whitelist = array('127.0.0.1', '::1', 'localhost');
    
    //if (isset($_SERVER['HTTP_HOST']) && !in_array($_SERVER['HTTP_HOST'], $whitelist)) {
    //    http_response_code(403);
    //    die();
    //}
    
    //if (isset($_SERVER['SERVER_NAME']) && !in_array($_SERVER['SERVER_NAME'], $whitelist)) {
    //    http_response_code(403);
    //    die();
    //}

?>